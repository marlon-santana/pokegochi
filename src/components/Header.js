import { Box, Typography } from '@material-ui/core';

export function Header () {

    return (
        <Box style={{backgroundColor: '#131426', color:'#fff', width: '100%', height:'150px'}}>
            <Typography style={{fontSize:100}}>TV ONLINE</Typography>
            
        </Box>
     
    );
}